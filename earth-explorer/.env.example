# Cesium Ion access token — get a free one at https://ion.cesium.com/tokens
# Without a token the app still runs, falling back to OpenStreetMap imagery
# and a smooth (non-terrain) ellipsoid, with 3D buildings disabled.
VITE_CESIUM_ION_TOKEN=
